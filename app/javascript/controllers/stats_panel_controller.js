import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // Placeholder controller so Stimulus can register `stats-panel` safely.
    // Add stats panel behavior here in the future if needed.
  }
}


